$(document).ready(function(){
    $('.editadmin').on('click',(function(e){
        console.log('adminnn');
       e.preventDefault();
    //    $.ajax({
    //       headers: {
    //           'X-CSRF-TOKEN': $('meta[name="_token"]').attr('content')
    //       }
      }));

      $('.editadmin').on('click',(function(e){
        console.log('adminnn');
       e.preventDefault();
    //    $.ajax({
    //       headers: {
    //           'X-CSRF-TOKEN': $('meta[name="_token"]').attr('content')
    //       }
      }));
    // });
  });

  